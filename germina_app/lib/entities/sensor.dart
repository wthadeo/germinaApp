import 'package:flutter/material.dart';

class Sensor extends StatelessWidget {
  const Sensor({ Key? key, required this.identification }) : super(key: key);

  final String identification;

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}