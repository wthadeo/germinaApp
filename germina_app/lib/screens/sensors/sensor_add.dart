import 'package:flutter/material.dart';

class SensorAdd extends StatelessWidget {
  const SensorAdd({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow,
    );
  }
}